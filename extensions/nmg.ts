// Stable Pi package entry point. The implementation remains in the project
// extension directory so existing local development installs keep working.
export { default } from "../.pi/extensions/nmg/index.ts";
