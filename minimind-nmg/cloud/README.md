# Cloud Training

Hand the agent `EXECUTION_BRIEF.md` and `ACCEPTANCE_CRITERIA.md`.

Agent will: setup env → download datasets → train → export ONNX.

```bash
# The agent just needs:
# - RTX 4090 (24GB) with CUDA 12.x
# - This repo (git clone or tarball)
# - Internet access for dataset download
```

Cost: ¥5 on AutoDL, 3 hours.
